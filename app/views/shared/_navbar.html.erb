<header class="d-flex justify-content-between py-2 position-fixed bg-none w-100 px-2">
  <h3 class="bebas text-uppercase fw-bold body-color">BrewUp</h3>

  <ul class="d-flex gap-2">
    <li class="list-unstyled">
      <a class="nav-link text-decoration-none body-color" href="">
        Brewery
      </a>
    </li>
    <li class="list-unstyled">
      <a class="nav-link text-decoration-none body-color" href="">
        Beers
      </a>
    </li>
    <% if user_signed_in? %>
      <li class="list-unstyled">
        <a class="nav-link text-decoration-none body-color" href="">
            <%= link_to "+ Add 🍺", new_beer_path, class:"nav-link px-2 text-white" %>
            <%= link_to "Profile", user_path(current_user), class: "dropdown-item" %>
            <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
          <% else %>
              <%= link_to "Login", new_user_session_path, class: "nav-link" %>
        </a>
      </li>
    <% end %>
  </ul>

</header>
